import { DivisionPipePipe } from './division-pipe.pipe';

describe('DivisionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DivisionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
