import { RepeatWordPipe } from './repeat-word.pipe';

describe('RepeatWordPipe', () => {
  it('create an instance', () => {
    const pipe = new RepeatWordPipe();
    expect(pipe).toBeTruthy();
  });
});
